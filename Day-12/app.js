// who's buying lunch

function whopaying (names){

    let totelPerson = person.lenght;
    let rendomPersonPosition = Math.floor(Math.person()*totelPerson);
    let rendomPerson = person[rendomPersonPosition];

    alert(rendomPerson + " is going to but lunch today") ;
}